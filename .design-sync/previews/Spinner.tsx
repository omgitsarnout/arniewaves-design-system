import { Spinner } from "@omgitsarnout/arniewaves-design-system";

export const Sizes = () => (
  <div style={{ display: "flex", gap: 18, alignItems: "center" }}>
    <Spinner size={18} />
    <Spinner size={26} />
    <Spinner size={36} />
  </div>
);
