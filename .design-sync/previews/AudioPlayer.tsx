import { AudioPlayer } from "@omgitsarnout/arniewaves-design-system";

export const DemoLoop = () => (
  <div style={{ maxWidth: 520 }}>
    <AudioPlayer
      title="AudioTeleporter — demo loop"
      artist="ArnieWaves"
      duration={184}
      seed={4}
    />
  </div>
);
